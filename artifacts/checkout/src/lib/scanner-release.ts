// Increment only for a tested scanner release. The build also emits this value
// outside the service-worker cache so an installed copy can compare versions.
export const SCANNER_RELEASE = "2026.09.10.3";
